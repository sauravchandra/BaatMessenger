<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_centerVertical="true"
    android:layout_gravity="center_vertical">

    <TextView
        android:layout_above="@+id/logo_background"
        android:id="@+id/welcome_text"
        android:layout_width="match_parent"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:text="Welcome to Baat Messenger"
        android:textAlignment="center"
        android:textSize="25sp" />

        <ImageView
            android:id="@+id/logo_background"
            android:layout_centerInParent="true"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:layout_height="300dp"
            android:src="@drawable/logo_background"/>

        <ImageView
            android:layout_alignTop="@+id/logo_background"
            android:layout_alignBottom="@+id/logo_background"
            android:layout_alignStart="@+id/logo_background"
            android:layout_alignEnd="@+id/logo_background"
            android:layout_centerHorizontal="true"
            android:id="@+id/imgLogo"
            android:layout_margin="50dp"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_marginTop="40dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ws_logo_white" />

        <TextView
            android:id="@+id/app_policy_tv"
            android:layout_above="@+id/continue_button"
            android:layout_width="match_parent"
            android:linksClickable="true"
            android:text="@string/app_policy_text"
            android:layout_centerHorizontal="true"
            android:layout_height="wrap_content"
            android:layout_marginTop="60dp"
            android:layout_marginLeft="15dp"
            android:layout_marginRight="15dp"
            android:textAlignment="center"
            android:textSize="12sp" />

        <Button
            android:id="@+id/continue_button"
            android:layout_centerHorizontal="true"
            android:layout_alignParentBottom="true"
            android:textAllCaps="false"
            android:layout_width="220dp"
            android:layout_height="40dp"
            android:layout_marginTop="5dp"
            android:layout_marginLeft="15dp"
            android:layout_marginRight="15dp"
            android:layout_marginBottom="15dp"
            android:background="@drawable/continue_button"
            android:onClick="continueButtonClick"
            android:text="Continue"
            android:textColor="@color/white"
            android:textSize="20sp" />

</RelativeLayout>